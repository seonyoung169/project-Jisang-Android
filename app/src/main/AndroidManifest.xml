<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dek.jisangbase">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".ApplicationController"
        android:allowBackup="true"
        android:icon="@mipmap/launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".sign.SignActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".sign.SignUser" />
        <activity android:name=".sign.SignStoreMgr" />
        <activity android:name=".MrgPage.MgrPageActivity" />
        <activity android:name=".market.MarketActivity" />
        <activity android:name=".MrgPage.MgrPageUpload" />
        <activity android:name=".Search.SearchActivity" />
        <activity android:name=".Search.ResultActivity" />
        <activity android:name=".ItemDetail.ItemDetailActivity" />
        <activity android:name=".LocationProductList.ProductListActivity" />
        <activity android:name=".ActivityRegisterdItem"/>
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>